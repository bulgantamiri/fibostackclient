---
features:
  - |
    The ``fibostack flavor list`` command now accepts two additional
    options, ``--min-disk`` and ``--min-ram``, to filter flavor by
    minimum disk and minimum RAM, respectively.
