---
features:
  - |
    Now user can add the description when user create's
    the role using fsc ``fibostack role create`` command.
    User can add the description by adding
    `--description <Description>` to fsc
    ``fibostack role create`` command.
