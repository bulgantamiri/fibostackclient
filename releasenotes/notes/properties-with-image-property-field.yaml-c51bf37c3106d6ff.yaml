---
features:
  - Support for image search via properties of image. Currently
    "fibostack server create --image-property" only takes image property.
    Now it can also search image via properties (user defined) too.
    Story https://storyboard.fibostack.org/#!/story/2007860.
