---
features:
  - Add ``fibostack image metadef namespace create`` command
    to create metadef namespace for the image service.
  - Add ``fibostack image metadef namespace delete`` command
    to delete image metadef namespace.
  - Add ``fibostack image metadef namespace set`` command
    to update metadef namespace for the image service.
  - Add ``fibostack image metadef namespace show`` command
    to show metadef namespace for the image service.
